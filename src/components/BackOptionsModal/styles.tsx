import styled from 'styled-components'

export const Content = styled.div`
    .close-modal-button {
        position: absolute;
        top: 30px;
        left: 680px;
        background: transparent;
        border: none;
    }

    .title-modal,
    .paragraph-modal {
        margin-bottom: 25px;
    }

    .paragraph-modal {
        color: var(--dark-gray);
    }
`